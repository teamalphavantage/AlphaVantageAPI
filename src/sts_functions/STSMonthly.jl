function execute_sts_monthly_api_call(user_model::AVKUserModel, stock_symbol::String; data_type::Symbol = :json, outputsize::Symbol = :compact, logger::Union{Nothing,AbstractLogger} = nothing)::(Union{T, Nothing} where T<:Any)

    # TODO - fill me in ...
    
end